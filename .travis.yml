language: node_js
sudo: required
dist: trusty
addons:
  firefox: latest
  apt:
    sources:
      - google-chrome
    packages:
      - google-chrome-stable
node_js:
  - '6'
before_script:
  - npm install -g bower 
  - npm install -g polymer-cli 
  - npm install -g web-component-tester 
  - npm install
  - bower install
script:
  - polymer serve
